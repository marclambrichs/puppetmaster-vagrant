:backends:
  - yaml

:hierarchy:
  - "fqdn/%{::fqdn}"
  - "devops/%{::application}"
  - "%{::environment}"
  - "%{::datacenter}"
  - "common"

:yaml:
  :datadir: "/var/lib/hiera/%{::environment}/hieradata"
